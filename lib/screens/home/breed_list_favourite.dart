import 'package:flutter/material.dart';

class BreedListFavourite extends StatelessWidget {
  const BreedListFavourite({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
